/*
const validator = require('validator');
const expect = require('chai').expect;


describe('Проверка подключаемой библиотеки', () => {
    describe('Проверка метода isEmpty', () => {
        it('Должен выдать true', () => {
            expect(validator.isEmpty('')).to.equal(true);
        });

        it('Должен выдать false', () => {
            expect(validator.isEmpty('bla')).to.equal(false);
        });
    });

    describe('Проверка метода isEmail', () => {
        it('Должен выдать true', () => {
            expect(validator.isEmail('bla@bla.com')).to.equal(true);
            expect(validator.isEmail('bla@yandex.ru')).to.equal(true);
            expect(validator.isEmail('bla@gmail.com')).to.equal(true);
            expect(validator.isEmail('bla.bla@gmail.com')).to.equal(true);
        });

        it('Должен выдать false', () => {
            expect(validator.isEmail('bla@com')).to.equal(false);
            expect(validator.isEmail('bla.com')).to.equal(false);
        });
    });

    describe('Проверка метода equals', () => {
        it('Должен выдать true', () => {
            expect(validator.equals('', '')).to.equal(true);
            expect(validator.equals('bla', 'bla')).to.equal(true);
        });

        it('Должен выдать false', () => {
            expect(validator.equals('bla', '')).to.equal(false);
            expect(validator.equals('bla', 'BLA')).to.equal(false);
            expect(validator.equals('bla', 'Bla')).to.equal(false);
        });
    });
});
*/
